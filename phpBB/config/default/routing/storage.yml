phpbb_storage_file_controller:
    path:               /download/{type}/{id}
    methods:            [GET, POST]
    defaults:
        _controller:    phpbb.storage.file.controller:handle
    requirements:
        type:           attachent|avatar|backup
        id:             \d+
