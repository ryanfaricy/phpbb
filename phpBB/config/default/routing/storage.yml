phpbb_storage_file_controller:
    path:               /download/{storage}/{id}
    methods:            [GET, POST]
    defaults:
        _controller:    phpbb.storage.file.controller:handle
    requirements:
        storage:           attachent|avatar|backup
        id:             \d+
